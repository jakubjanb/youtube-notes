# Notes

Final notes live here, grouped by primary domain.

Use:

```text
notes/<domain>/<note-slug>/
```

Create a new note folder with:

```bash
make new-note TITLE="Gradient Descent Intuition" DOMAIN=machine-learning
```

Each note folder should contain metadata, transcript, blueprint, Markdown companion, LaTeX source, compiled PDF when available, figures, and attachments.

