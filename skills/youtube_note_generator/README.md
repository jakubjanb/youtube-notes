# youtube_note_generator

This folder is a project-level pointer for the `youtube_note_generator` workflow.

The existing implementation/reference folder in this workspace is:

```text
youtube-note-generator/
```

Use the skill to convert a blueprint into a polished LaTeX note stored at:

```text
notes/<domain>/<note-slug>/note.tex
```

