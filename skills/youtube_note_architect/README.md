# youtube_note_architect

This folder is a project-level pointer for the `youtube_note_architect` workflow.

The existing implementation/reference folder in this workspace is:

```text
youtube-note-architect/
```

Use the skill to convert a YouTube transcript and metadata into a blueprint stored at:

```text
blueprints/<domain>/<note-slug>.md
```

