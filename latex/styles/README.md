# LaTeX Styles

Use this folder for future project-specific `.sty` files if the shared LaTeX setup grows beyond the template files.

The current project keeps reusable LaTeX configuration in:

```text
latex/templates/
```

