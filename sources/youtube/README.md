# YouTube Sources

This folder stores canonical YouTube source material.

- `metadata/` contains one YAML file per source.
- `transcripts/` contains raw or cleaned transcript Markdown files.
- `raw/` stores optional raw downloads or exports when needed.

Use matching slugs across metadata, transcript, blueprint, and final note folders whenever possible.

